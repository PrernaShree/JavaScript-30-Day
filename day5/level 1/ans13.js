const itCompanies = ['Facebook', 'Google', 'Microsoft', 'Apple', 'IBM', 'Oracle', 'Amazon'];
let input = "tesla";
if(itCompanies.includes(input))
{
	console.log(input);
}
else
{
	console.log(input , " This company not included in array.");
}